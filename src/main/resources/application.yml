server:
  port: 8083

spring:
  application:
    name: sentinel-example
  cloud:
    sentinel:
      transport:
        dashboard: localhost:8080

    nacos:
      discovery:
        server-addr: localhost:8848



